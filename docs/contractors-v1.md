# Deprecated: Contractors v1 Implementation Notes

This document is deprecated.

Use `docs/contractor-workspace-prd.md` as the canonical contractor source of truth for production implementation, including contractor profiles, onboarding, WorkOS invite/claim, identity resolution, contractor workspace routes, evidence, schedule, authorization, and tests.

The old Contractors v1 implementation notes were retained only as prior codebase context during creation of the new PRD and must not be treated as an active product or implementation spec.
