# Lender Portal MVP — Phase 9 Tracker

Baseline: branch `08-15-phase-6`, commit `62b8eb75e9c1ebfa6ac3323a38e303d9af2b1df4`.

This tracker records only confirmed Phase 9 defects, risks, decisions, and dispositions. It does not replace the authoritative specification, implementation plan, work packages, migration plan, security review, release plan, or certification evidence.

## Active entries

| ID | Category | Severity | Evidence | Affected phase/files | Disposition | Owner | Blocks implementation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LP9-STEER-001 | Steering decision | N/A | Preflight found branch `08-15-phase-6` at `62b8eb75e9c1ebfa6ac3323a38e303d9af2b1df4` with only untracked `cmux-procedural-agent-access.html`. | Entire Phase 9 worktree | Preserve the user-owned file. Do not clean, reset, stash, delete, overwrite, commit, push, or misattribute it. | Orchestrator and implementation task | No |
| LP9-STEER-002 | Steering decision | N/A | Phase 9 must productionize the Phase 8 vertical through canonical Phase 1–8 lifecycle, WorkOS, ACL, assignment, eligibility, audit, communication, migration, and transport seams. | `convex/production_proposals.ts`, `convex/lender_portal_phase5.ts`, `convex/lender_portal_notifications.ts`, `convex/quote_notifications.ts`, `convex/schema.ts` | Do not create a parallel lifecycle, identity, membership, assignment, recipient, authorization, notification, migration, or release-authority system. | Implementation task | Yes if violated |
| LP9-STEER-003 | Steering decision | N/A | The user requires visible independent Codex tasks, exactly one GPT-5.6 Sol/high implementer, GPT-5.6 Luna/max advisory tasks, no sub-agents, no workpackage execution framework, no exact-SHA QA process, and no `sol-advisor:orchestration`. | Phase 9 orchestration | Follow the requested visible-task topology. Advisory tasks report to the orchestrator and do not steer the implementer directly. | Orchestrator | No |
| LP9-MIG-001 | Confirmed certification blocker | Critical | No Phase 9 candidate-data inventory, source-field provenance report, dry run, idempotent replay, partial-failure recovery, authenticated readback, audit record, or pre/post projection reconciliation exists at the baseline. | `convex/schema.ts`, `convex/migrations.ts`, domain `*_migrations.ts`, proposal and queue/dashboard projections | Implement the authorized fail-closed migration/reconciliation vertical using repository migration APIs. Stop on ambiguity, preserve history and WorkOS ownership, and prove counts, invariants, replay, failure handling, and recovery. | Implementation task | Yes |
| LP9-SEC-001 | Confirmed certification blocker | Critical | Phase 8 has inherited focused authorization tests, but no Phase 9 exact-candidate review covers every query, command, attachment URL, email link, historical-withdrawn path, stale assignment/cycle/revision, deactivation/reassignment, race, secret, log, and private-payload boundary. | Lender Portal commands/loaders/routes, communication worker/transport, audit and observability seams | Run the Phase 9 security/privacy/concurrency review and add focused canonical-public-seam tests for confirmed gaps only. Reuse existing ACL and WorkOS projection owners. | Implementation task | Yes |
| LP9-REL-001 | Confirmed release-control defect | Critical | The baseline has no verified lender-portal tenant-scoped release-control state, authorized operator entry point, auditable enable/disable changes, safe default, canary/allowlist behavior, or operational readback governing the Phase 8 delivery path. | Canonical tenant configuration/release-control owner, `convex/lender_portal_notifications.ts`, `convex/quote_notifications.ts`, worker/cron path | Extend the canonical tenant control seam and gate the existing Phase 8 path. Prove disabled, enabled/canary, draining, and rollback behavior without creating a second notification authority. | Implementation task | Yes |
| LP9-REL-002 | Confirmed release-risk gap | Critical | Queued, claimed/in-flight, failed, suppressed, and retryable communication intents have no Phase 9-proven behavior when a tenant is disabled or draining. | `communicationIntents`, `communicationAttempts`, `communicationOutcomes`, provider reservation/worker/retry code | Preserve durable records; stop new claims; recheck immediately before provider submission; explicitly abandon/suppress stale claims; pause retries; resume through existing idempotency and current authorization; reconcile already-in-flight provider results without duplicate sends. | Implementation task | Yes |
| LP9-OBS-001 | Confirmed certification blocker | High | No Phase 9 evidence defines or proves tenant-scoped release state visibility, notification health metrics, actionable alert ownership/thresholds, sensitive-data-safe logs, migration health, or rollback observability. | Operational queries, audit events, runbooks, dashboards/alerts, communication health | Implement or document the authorized observable seams and prove they expose operational state without private payloads or secrets. | Implementation task | Yes for release certification |
| LP9-CERT-001 | Confirmed certification blocker | Critical | Phase 8 automated validation passed, but browser QA, inbox rendering, live Resend/provider validation, deployment configuration validation, manual tenant rollout, and rollback drill were explicitly deferred. | Phase-end validation and evidence artifacts | Collect authorized external/manual evidence at phase end. Until then, certification must remain bounded to automated evidence. | Orchestrator and implementation task | No for code implementation; yes for production certification |
| LP9-RISK-001 | Unresolved risk | High | The authoritative plan requires actual candidate-environment data inventory before migration behavior is finalized; no candidate data or production authorization has been supplied in this task. | Migration inventory, rehearsal, authenticated readback, production execution | Build safe preview/rehearsal and tests from representative fixtures. Do not execute destructive or production mutations without explicit authorization and verified rollback/forward-recovery evidence. | Orchestrator | No for implementation; yes for production execution |

## Review decisions

| ID | Decision | Evidence | Disposition |
| --- | --- | --- | --- |
| LP9-REVIEW-001 | Bind Phase 9 to `62b8eb75e9c1ebfa6ac3323a38e303d9af2b1df4`. | The Phase 8 tracker records an earlier implementation start at `41f287f6`; the actual committed Phase 8 result and current HEAD are `62b8eb75`. | Use the later committed baseline for implementation and evidence. Treat Phase 8 tracker claims as inherited evidence, not Phase 9 certification. |
| LP9-REVIEW-002 | Approval, closing, and activation remain distinct. | Phase 9 plan requires migration of proposal states without accidentally activating approved proposals. | Any ambiguous proposal row must stop and surface a migration issue; never infer activation. |
| LP9-REVIEW-003 | WorkOS projection tables remain webhook-owned. | Repository Convex rules prohibit product flows, migrations, tests, and provisioning shortcuts from writing WorkOS projections. | Migrations may read projections for verification but must not write them. |
| LP9-REVIEW-004 | Notification release control governs claims and delivery without deleting intent history. | Phase 8 owns transactional durable intent creation; Phase 9 owns controlled exposure and delivery health. | Preserve transactional intent creation and current delivery-time authorization. Disabled/draining tenants retain auditable intents while claims/retries fail safely. |

## Advisory remediation dispositions

| Finding | Severity | Landed evidence | Disposition |
| --- | --- | --- | --- |
| 1. Migration inventory tenant isolation | P1 | `buildProposals.by_brokerage_and_organization` scopes before child reads; authenticated inventory verifies exact organization and Brokerage; shared-Brokerage negative fixture excludes foreign proposal metadata. | Closed in code and focused tests. |
| 2. Policy, revision, approval, assignment, and lock linkage | P1 | The Phase 9 runner now validates proposal, organization, Brokerage, assignment, lender organization, confirmation cycle, revision, policy, closing, lock, and active-Build scope before patching. Contradictory pointers persist actionable issues and remain unchanged. | Closed in code and focused tests through the apply gate. |
| 3. Exact migration execution provenance | P1 | `lenderPortalPhase9MigrationRuns` persists exact candidate/configuration, bounded counts, fingerprinted records/issues, WorkOS projection fingerprint/count, run token, pre/post state, and status. Apply requires the sole globally active authorized token and disabled exact tenant release target; unrelated tenants no-op. Verification proves projections, zero pending reconciliation, issue-free readback, and WorkOS non-write by fingerprint. | Closed in code and focused tests. Actual candidate-environment execution remains a release gate. |
| 4. Disable/provider-send race | P1 | Release `accessRevision` is copied into the durable provider reservation in the same authorization transaction. Draining stops claims. Disabled cannot commit while an active or expired/unknown provider reservation lacks a durable outcome. Lease expiry alone never clears the interlock. | Closed in code and interleaving tests. |
| 5. Attempt sampling false green | P2 | Attempts are loaded only for selected Lender Portal intents; attempt truncation sets `incompleteSampling` and prevents `terminalHealthy`. | Closed in code and focused tests. |
| 6. Health domain scoping | P1 | Four canonical Lender Portal intent kinds use the organization+kind+created-at index before the 500-row bound; unrelated quote traffic cannot consume the sample. | Closed in code and focused tests. |
| 7. Complete release audit prior/new state | P2 | Release audit stores and returns privacy-safe complete prior/new status, access revision, candidate SHA, configuration hash, canary count/hash, reservation count, actor roles, reason, warnings, and correlation. | Closed in code and focused tests. |
| 8. Actionable migration readback | P1 | Run-token-scoped paginated issue readback returns source table, source record ID, field, reason, provenance, and persisted disposition with totals bound to the manifest. | Closed in code and focused tests. |
| 9. Reachable operator interface and generated API | P1 | Authenticated public functions are generated under `api.lender_portal_release` and `api.lender_portal_phase9`; the registered internal migration runner is documented. Phase 9 tests use generated references with no `api as any` or `internal as any`. | Closed in code, codegen, tests, and `docs/runbooks/lender-portal-phase9-release.md`. |
| 10. Validated-time and terminal health | P1 | Health accepts and validates the operator-runtime epoch, alerts on the first failed attempt, measures terminal latency, and exposes active, expired, and reconciliation-required provider reservations without payloads. No `Date.now()` remains in the query. | Closed in code and focused tests. |
| 11. Release-gate pause outcome | P1 | A paused claimed attempt becomes terminal `abandoned`; the same intent returns to its exact pending/retry state; one idempotent privacy-safe outcome records intent, attempt, release revision, and reason. | Closed in code and focused tests. |
| 12. Complete paginated audit provenance | P1 | Release audit queries the release entity index before pagination. Migration audit queries organization+run token before pagination and verifies the run/Brokerage. Both return complete privacy-safe provenance rather than post-filtered samples. | Closed in code and focused tests. |
| 13. Trusted runtime candidate/configuration | P1 | Canary and enable require exact `LENDER_PORTAL_RELEASE_CANDIDATE_SHA` and `LENDER_PORTAL_RELEASE_CONFIGURATION_HASH` values from the running deployment. Missing or mismatched proof fails closed. | Closed in code and focused tests. Deployment evidence remains a phase-end gate. |
| 14. Operational drain and rollback | P1 | Draining stops claims, exposes active and expired/unknown reservations, preserves provider completion/retry idempotency, and gates disabled on zero unresolved reservations. Audit preserves previous target/configuration. The supported contract is history-preserving disable plus forward recovery. | Closed in code, focused tests, and operator runbook. External rollback drill remains a phase-end gate. |

## Fixed-point follow-up dispositions

| Finding | Landed evidence | Disposition |
| --- | --- | --- |
| 1. Exact active WorkOS organization | `requireTenantReleaseOperator` now requires the exact active `workosOrganizations` projection in addition to active user, membership, role, token, and Brokerage. AuthKit organization deletion is covered. | Closed. |
| 2. One exact-scope snapshot validator | `collectMigrationSnapshot` is the shared preparation, authorization, apply-preflight, and verification inventory. It blocks lifecycle, review-cycle target, Builder, policy/revision/lock, assignment, and existing Kanban contradictions. The projection migration creates missing cards but never rewrites a contradictory card. | Closed. |
| 3. Tenant-scoped lender reconciliation | New rows persist `organizationId`; readback/blocking uses exact Brokerage+organization+status; deterministic dedup uses the bounded `by_scope_source` index. Same-Brokerage foreign candidates do not block the tenant. | Closed. |
| 4. Expired provider outcome ambiguity | Active reservations become `expired` after the lease but remain draining/disable interlocks. Only an idempotent durable attempt outcome releases them; authorization cannot create a duplicate reservation/provider call. Health reports `reconciliationRequired`. | Closed. |
| 5. Health query time source | `getLenderPortalOperationalHealth` accepts a validated safe-integer epoch and contains no `Date.now()`. Callers and runbook pass operator-runtime time. | Closed. |
| 6. Bounded candidate dedup | Reconciliation dedup uses `by_scope_source(...).take(2)` and fails on contradictory duplicates; no unbounded candidate collect remains. | Closed. |
| 7. Apply-time lifecycle contradictions in preflight | Missing explicit approval time, non-closed closing/Build evidence, closing timestamp conflict, duplicate/missing closing, active-Build pointer conflict, and duplicate Builds are persisted before authorization/apply writes. | Closed. |
| 8. Complete manifest fingerprint | The manifest hashes complete proposal, Builder, closing, Build, assignment, approval, policy, revision, lock, card, review-cycle, issue, reconciliation-candidate, and every dereferenced decision-dependency row. WorkOS rows retain their separate complete fingerprint/count. | Closed. |
| 9. Deterministic prepare replay | Preparation checks the organization+run-token index with a two-row duplicate guard and returns the existing manifest. Focused readback proves one row after replay. | Closed. |
| 10. Runtime provenance and release interlock | Inventory, prepare, authorize, apply preflight, every runner row, verify, canary, and enable recheck runtime SHA/configuration. Canary/enable rejects an active authorized/applying tenant migration. Runner and verify recheck exact disabled release state. | Closed. |
| 11. Explicit-only approval evidence | Lifecycle reconciliation never derives `approvedAt` from revision creation. Missing proposal approval time or approval decision time/cycle evidence is an actionable ambiguity and no write occurs. | Closed. |
| 12. Durable blocked-authorization audit | Snapshot/WorkOS drift patches the run blocked and records expected/observed fingerprints, counts/issues, warnings, actor, reason, candidate/configuration, run token, and prior/new status. | Closed. |

## Automated implementation evidence

- Generated API: `bun x convex codegen`; `convex/_generated/api.d.ts` includes `lender_portal_phase9` and `lender_portal_release`.
- Phase 9 focused suite: `bun x vitest run convex/lender_portal_phase9.test.ts` — 1 file passed, 10 tests passed, 0 failed.
- Inherited migration regression selection: `bun x vitest run convex/production_proposals.test.ts -t 'refuses to fabricate legacy policy|backfills an exact approval revision|same creation time|prior reviewed revision|multiple-current-assignment migration ambiguity|closed Build policy lock|historical external quorum|operator activation override|ambiguous historical approval revision'` — 1 file passed, 9 tests passed, 204 skipped, 0 failed.
- Convex TypeScript: `bun x tsc -p convex/tsconfig.json --noEmit` — passed with no diagnostics.
- Patch hygiene: `git diff --check` — passed with no diagnostics.
- Focused Phase 9 suite exercises authenticated public seams, the registered migration component/runner, current worker seams, and canonical WorkOS AuthKit projection events. It does not write WorkOS projection tables directly.
- Focused coverage includes cross-tenant inventory and reconciliation-candidate denial; active WorkOS organization deactivation; contradictory approval/lock/lifecycle/review-cycle/Kanban links; complete snapshot and WorkOS drift audit; deterministic prepare replay; runtime/candidate/configuration mismatch; dry run, complete runner apply, partial-resume mechanism, and no-op replay; active and expired provider reservations versus drain/disable; idempotent provider-result recovery; incomplete attempt samples; failed-attempt alerts; complete release audit state; and privacy-safe release-gate outcomes.
- Phase-end broad-suite triage covered all 24 reported failures against an isolated source snapshot of baseline `62b8eb75e9c1ebfa6ac3323a38e303d9af2b1df4` using the same installed dependencies:
  - Phase 9 caused 2 failures. The archive regression came from applying the lender-portal durable-outcome interlock to a known quote reservation; explicit non-lender release is restored while unknown and lender-portal reservations still fail closed. The inherited Phase 4 delivery test had no enabled tenant release fixture; it now binds the existing current tenant intent to an explicit enabled control. Current recipient/resource authorization also runs before the tenant release decision at claim and provider authorization.
  - The exact Phase 9 regressions now pass: data retention 1 passed / 13 skipped; inherited Phase 4 plus migration selection 10 passed / 203 skipped.
  - 21 failures reproduced unchanged on the clean baseline: 17 backend failures across `active_build_submilestone_completion_review`, `assistant`, `build_action_items`, `build_collaboration_focus`, `build_collaboration_lifecycle`, and `build_collaboration_operational_events`; 4 frontend failures across `app-shared`, `useBuildDetailTargetController`, and the Builder Build route. These files and their failing runtime owners are outside the Phase 9 diff.
  - The remaining reported timeline-workspace failure did not reproduce in either the active isolated run or the clean-baseline isolated run; the timeline file passed in both four-file frontend selections. It is classified as a broad-suite-only pre-existing/order-sensitive failure, not a confirmed Phase 9 regression.
- Post-remediation Phase 9 focused suite: 1 file passed, 10 tests passed, 0 failed. Its release-control test now uses canonical proposal/revision/confirmation-cycle correlation, application-owned lender assignments, and AuthKit-projected WorkOS recipients so current authorization is exercised before release gating.
- Final generated API and checks: `bun x convex codegen` passed; `bun x tsc -p convex/tsconfig.json --noEmit` passed; Phase 9 generated API escape scan passed; `git diff --check` passed.
- Broad application build, broad regression, browser/inbox QA, live provider/webhook validation, deployment validation, actual candidate data execution, rollback drill, alert routing, and final certification remain deliberately deferred to phase-end validation.

## Final disposition

Phase 9 implementation findings 1–14 and fixed-point findings 1–12 are code-complete with focused automated evidence. Production certification remains pending the explicitly deferred phase-end external, deployment, broad-regression, and independent-review gates.
