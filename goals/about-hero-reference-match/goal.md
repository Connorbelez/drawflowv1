# About Hero Reference Match

Implement the `/about` page above-the-fold desktop composition so it visually matches the supplied CleanShot reference, including the hero, visible Who We Are area, and What We Finance region. Use the existing `public/assets/about-webp` assets and paper grain overlays, with `paper-grain-overlay.webp` applied to the hero text/paper treatment.

Use `facts.md` as the shared understanding of what must be true.

Use `plan.md` as the approved execution plan.

Done means the implementation is scoped to `/about` and related styling/tests, targeted automated checks pass, and repeated browser screenshots at the reference desktop viewport are materially aligned with the supplied reference under the accepted visual-fidelity standard.
