node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vercel
.vinxi
playwright-report/
__unconfig*
todos.json
tmp/
# Convex generated bindings are imported by Vite/TanStack routes at build time.
# Keep them tracked so clean Vercel clones can compile without running Convex
# codegen before the app bundle is built.
convex/_generated/ai/

# Impeccable live mode config is source, but runtime journals are not.
.impeccable/live/sessions/
.impeccable/live/annotations/
.impeccable/live/server.json
.impeccable-live/
.impeccable-live.json
